{% extends "base.twig" %}

{% block body %}

  <p class="lead">
    Post to your Github-hosted blog just by sending an email.
  </p>

  <p>
    <a class="button button-primary button-block"
       href="{{ path('github_connect') }}"
       data-ga="{{ {'category':'button','action':'click','label':'home-connect'} | json_encode() }}">
      Connect with GitHub
    </a>
  </p>

  <p class="text-center">
    <a href="{{ path('about') }}"
       data-ga="{{ {'category':'button','action':'click','label':'home-about'} | json_encode() }}">
      How does it work?
    </a>
  </p>
{% endblock %}